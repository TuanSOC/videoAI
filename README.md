# vidgen

Topic → faceless video (short 9:16 / long 16:9, vi/en). Stock footage + local AI visuals, edge-tts, FFmpeg.

Setup and usage docs are written in phase 7. For now:

```
python -m uv sync
python -m uv run python -m vidgen doctor
```
